> **Note**
> [Go to the end](#sphx-glr-download-auto-examples-cython-plot-06-multifile-support-files-py)
to download the full example code or to run this example in your browser via JupyterLite or Binder.

# Multi-file builds: .pxi includes and external headers[#](#multi-file-builds-pxi-includes-and-external-headers "Link to this heading")

Real projects often split Cython code across multiple files:

* `.pxi` for shared Cython fragments (`include "helper.pxi"`)
* `.pxd` for shared declarations (usually used with `cimport` across modules)
* `.h` for C-level declarations used via `cdef extern from ...`

This single-module example uses `support_files` to ship extra files into the
build dir without touching your repository.

## Important[#](#important "Link to this heading")

A `.pxd` file alone does not create a runtime module. Using `cimport name`
requires that `name` is an actual compiled module. To keep this demo as a
single-module build, we include the `.pxd` textually instead of `cimport`.
See the package-build note below for the real multi-module `cimport` pattern.

```
# Authors: The scikit-plots developers
# SPDX-License-Identifier: BSD-3-Clause

```
```
from __future__ import annotations

from scikitplot import cython

```

## Generate `python` Module from `cython`[#](#generate-python-module-from-cython "Link to this heading")

```
report = cython.check_build_prereqs(numpy=False)

if not report.get('cython', {}).get('ok'):
    print("Skipping compilation because build prerequisites are missing.")
    problems = report.get("problems", [])
    if problems:
        print("Problems:", problems)
else:
    support_files = {
        # Declarations file (used as textual include in this single-module demo).
        "_helper_decl.pxd": "cdef inline int square_d(int x):\n    return x * x\n",
        # Implementation fragment (text include).
        "_helper_square.pxi": "cdef int square_i(int x):\n    return x * x\n",
        # C header (extern usage).
        "_helper_add.h": "static int add_i(int a, int b) { return a + b; }\n",
    }

    code = r"""
# distutils: language = c

# Single-module demo: include .pxd textually for declarations (no runtime import).
from _helper_decl cimport square_d

include "_helper_square.pxi"

cdef extern from "_helper_add.h":
    int add_i(int a, int b)

def squared(int n):
    return square_d(n)

def square(int n):
    return square_i(n)

def add(int a, int b):
    return add_i(a, b)
"""

    r = cython.compile_and_load_result(
        code,
        module_name="multifile_demo",
        profile="fast-debug",
        support_files=support_files,
        compiler_directives={"language_level": 3, "embedsignature": True},
        verbose=0,
    )

    print("squared(7) =", r.module.squared(7))
    print("square(7)  =", r.module.square(7))
    print("add(2,3)   =", r.module.add(2, 3))

```
```
squared(7) = 49
square(7)  = 49
add(2,3)   = 5

```

Tags: [domain: cython](../../_tags/domain-cython.html) [plot-type: cython](../../_tags/plot-type-cython.html) [purpose: showcase](../../_tags/purpose-showcase.html)

****Total running time of the script:**** (0 minutes 0.551 seconds)

[![Launch binder](../../_images/binder_badge_logo5.svg)](https://mybinder.org/v2/gh/scikit-plots/scikit-plots/main?urlpath=lab/tree/notebooks/auto_examples/cython/plot_06_multifile_support_files.ipynb)[![Launch JupyterLite](../../_images/jupyterlite_badge_logo5.svg)](../../lite/lab/index.html?path=auto_examples/cython/plot_06_multifile_support_files.ipynb)

[`Download Jupyter notebook: plot_06_multifile_support_files.ipynb`](../../_downloads/fc0324d0e748e6bfa7dea4742d877ec8/plot_06_multifile_support_files.ipynb)

[`Download Python source code: plot_06_multifile_support_files.py`](../../_downloads/ede1581b1a52096bedb9178b7f08e991/plot_06_multifile_support_files.py)

[`Download zipped: plot_06_multifile_support_files.zip`](../../_downloads/83c1eb81758ee808c219f3f999630702/plot_06_multifile_support_files.zip)

Related examples

![](../../_images/sphx_glr_plot_07_cpp_mode_basics_thumb.png)

[C++ mode basics: cppclass and libcpp containers](plot_07_cpp_mode_basics.html)

C++ mode basics: cppclass and libcpp containers![](../../_images/sphx_glr_plot_09_workflow_templates_cli_thumb.png)

[Workflow templates (train / hpo / predict) + CLI entry template](plot_09_workflow_templates_cli.html)

Workflow templates (train / hpo / predict) + CLI entry template![](../../_images/sphx_glr_plot_08_vector_ops_without_numpy_thumb.png)

[Vector ops without NumPy: array(‘d’) + memoryviews](plot_08_vector_ops_without_numpy.html)

Vector ops without NumPy: array('d') + memoryviews![](../../_images/sphx_glr_plot_mmap_script_thumb.png)

[Mmap annoy.AnnoyIndex with examples](../annoy/plot_mmap_script.html)

Mmap annoy.AnnoyIndex with examples

[Gallery generated by Sphinx-Gallery](https://sphinx-gallery.github.io)